# 実設定(Webhook URL などの機密を含む)
config.json

# ビルド成果物
/gaya-talk
/bin/
build/*.app/
/dist/

# モデルファイル(大きいのでコミットしない)
*.bin

# ローカル環境(グローバル ignore 頼みにしない: 他の環境でも誤コミットを防ぐ)
.DS_Store
.envrc

# 初期設計の記録(手元にだけ残す。現行仕様は development.md と config.example.json が正)
docs/room-overlay-design.md
